package com.lancas.vs_wap.content.blocks.cartridge;

//public interface ICollisionDetector {
    //public static double PREDICT_TIME = 0.2;
    //public static double PLANE_RAYCAST_STEP_WORLD = 0.5;






    //get the bounds in local space(local to block lower corner)
    /*public boolean shouldDetect(BlockState state);
    public AABBd getLocalBound(BlockState state);  //get the bound of VoxelShape
    public CollisionDetectMethod getDetectType(BlockState state);
    public void onCollision(Level level, BlockPos detectorBp, BlockState detectorState, BallisticsHitInfo hitInfo);
     */




    /*public default AABBd getAABBInWorld(Matrix4dc shipToWorld, BlockPos blockPos) {
        AABBd aabbInShip = getBound().translate(JomlUtil.dLowerCorner(blockPos), new AABBd());
        AABBd aabbInWorld = BallisticsUtil.quickTransformAABB(shipToWorld, aabbInShip, new AABBd());

        return aabbInWorld;
    }
    public default AABBd getPredictAABBInWorld(Matrix4dc shipToWorld, BlockPos blockPos, Vector3dc movement) {
        return BallisticsUtil.extendAlong(getAABBInWorld(shipToWorld, blockPos), movement);
    }*/
//}
