package com.lancas.vs_wap.content.blocks.abstrac;

//set POWERED
/*
public interface IPowerSrc {
    public static final IntegerProperty POWER = BlockStateProperties.POWER;

    public default

    public default int getSignal(BlockState state, BlockGetter blockAccess, BlockPos pos, Direction side) {
        return state.getValue(POWER);
    }
    public default boolean isSignalSource(BlockState state) {
        return true;
    }
    public default int getDirectSignal(BlockState state, BlockGetter blockAccess, BlockPos pos, Direction side) {
        return getSignal(state, blockAccess, pos, side);
    }
}*/