package com.lancas.vs_wap.content.items;

/*
import com.lancas.vs_wap.ship.data.ShipSchemeDataAsTag;
import net.minecraft.server.level.ServerLevel;
import net.minecraft.world.item.Item;
import net.minecraft.world.item.ItemStack;
import org.valkyrienskies.core.api.ships.ServerShip;

public class GreenPrint extends Item {
    public GreenPrint(Properties p_41383_) {
        super(p_41383_);
    }

    public static ItemStack saveScheme(ItemStack stack, ServerLevel level, ServerShip ship) {
        ShipSchemeDataAsTag schemeData = new ShipSchemeDataAsTag();
        schemeData.readShip(level, ship);
        stack.getOrCreateTag().put("ship_data", schemeData.toTag());
        return stack;
    }




}
*/