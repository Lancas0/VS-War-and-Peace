package com.lancas.vs_wap.obsolete.item;
/*
import com.lancas.vs_wap.debug.EzDebug;
import net.minecraft.world.item.Item;
import net.minecraft.world.item.ItemStack;

public class ShipSchemeRepairItem extends Item {

    //public static final ShipSchemeRepairItem DEFAULT_ITEM = new ShipSchemeRepairItem(new Properties().stacksTo(1).durability(500));

    public ShipSchemeRepairItem(Properties p_41383_) {
        super(p_41383_);
    }


    public static void damageByRepair(ItemStack repairkStack, ItemStack schemeStack) {
        int damagedValue = ShipSchemeItem.getDamagedValue(schemeStack);
        int newRepairItemDamage = repairkStack.getDamageValue() + damagedValue;

        repairkStack.setDamageValue(newRepairItemDamage);

        if (newRepairItemDamage >= repairkStack.getMaxDamage()) {
            repairkStack.shrink(1);
            EzDebug.log("Repair kit is borken");
        }
    }

}
*/