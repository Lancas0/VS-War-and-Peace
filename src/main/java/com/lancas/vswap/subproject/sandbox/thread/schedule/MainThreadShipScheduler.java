package com.lancas.vswap.subproject.sandbox.thread.schedule;

/*
import com.lancas.vs_wap.subproject.sandbox.api.ConcurrentLifeTick;
import com.lancas.vs_wap.subproject.sandbox.api.ISavedObject;
import com.lancas.vs_wap.subproject.sandbox.ship.ISandBoxShip;
import net.minecraft.world.level.Level;

import java.util.concurrent.atomic.AtomicBoolean;

public abstract class MainThreadShipScheduler<TSelf extends MainThreadShipScheduler<TSelf, TShip>, TShip extends ISandBoxShip>
    implements ISavedObject<TSelf> {
    //protected ConcurrentLifeTick scheduleTicker;
    protected AtomicBoolean canceled = new AtomicBoolean(false);

    protected MainThreadShipScheduler() {}
    public MainThreadShipScheduler(int scheduleTicks) {
        ship = inShip;
        scheduleTicker = new ConcurrentLifeTick(scheduleTicks);
    }

    public abstract void onSchedule(Level level, TShip schedulingShip,);
}
*/