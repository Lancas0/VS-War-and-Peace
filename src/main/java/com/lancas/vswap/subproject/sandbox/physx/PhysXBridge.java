package com.lancas.vswap.subproject.sandbox.physx;
/*
public class PhysXBridge {
    static {
        System.loadLibrary("physxjni"); // 加载 JNI 库
    }

    // 初始化 PhysX
    public native void init();

    // 创建盒子碰撞体
    public native long createBoxCollider(
        float posX, float posY, float posZ,
        float halfSizeX, float halfSizeY, float halfSizeZ
    );

    // 执行物理模拟
    public native void simulate(float deltaTime);

    // 获取位置
    public native float[] getPosition(long actorHandle);
}*/