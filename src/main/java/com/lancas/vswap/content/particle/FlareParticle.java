package com.lancas.vswap.content.particle;

/*
public class FlareParticle extends TextureSheetParticle {
    public class FlareParticleType extends ParticleType implements ParticleOptions {
        @Override
        public ParticleType<?> getType() { return this; }
        @Override
        public void writeToNetwork(FriendlyByteBuf buf) {

        }
        @Override
        public String writeToString() {
            return "";
        }
        @Override
        public Codec codec() {
            return null;
        }
    }

    public static class Factory implements ParticleProvider<SimpleParticleType> {
        private final SpriteSet sprites;

        public Factory(SpriteSet sprites) {
            this.sprites = sprites;
        }

        @Override
        public Particle createParticle(SimpleParticleType type, ClientLevel level,
                                       double x, double y, double z,
                                       double dx, double dy, double dz) {
            return new FlareParticle(level, x, y, z, dx, dy, dz, sprites);
        }
    }
}
*/