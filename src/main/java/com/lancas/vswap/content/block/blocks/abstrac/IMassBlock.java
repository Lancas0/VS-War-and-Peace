package com.lancas.vswap.content.block.blocks.abstrac;

/*
import net.minecraft.world.level.block.state.BlockState;

public interface IMassBlock {
    public static double massOf(BlockState state) {
        if (state.getBlock() instanceof IMassBlock massBlock)
            return massBlock.getMass(state);
        return 1000;
    }

    public double getMass(BlockState state);
}
*/