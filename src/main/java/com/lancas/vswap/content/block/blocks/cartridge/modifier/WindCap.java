package com.lancas.vswap.content.block.blocks.cartridge.modifier;

/*
import com.lancas.vswap.ship.type.ProjectileWrapper;
import com.lancas.vswap.subproject.blockplusapi.blockplus.BlockPlus;
import com.lancas.vswap.subproject.blockplusapi.blockplus.adder.IBlockAdder;
import com.lancas.vswap.content.block.blocks.blockplus.DefaultCartridgeAdder;
import net.minecraft.core.BlockPos;
import net.minecraft.world.level.block.state.BlockState;

import java.util.List;

public class WindCap extends BlockPlus implements IModifier {
    public static List<IBlockAdder> providers = List.of(
        new DefaultCartridgeAdder()
        //, EinherjarBlockInfos.drag_factor.getOrCreateExplicit(WindCap.class, state -> 1.0),
        //EinherjarBlockInfos.mass.getOrCreateExplicit(WindCap.class, state -> 75.0)
    );

    @Override
    public List<IBlockAdder> getAdders() { return providers; }

    public WindCap(Properties p_49795_) {
        super(p_49795_);
    }

    @Override
    public double getAirDragMultiplier(ProjectileWrapper projectile, BlockPos pos, BlockState state) {
        if (projectile.headBp.equalsBp(pos))
            return 0.01;
        return 1;
    }
}
*/