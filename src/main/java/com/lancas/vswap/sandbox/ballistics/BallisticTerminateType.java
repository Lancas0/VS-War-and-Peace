package com.lancas.vswap.sandbox.ballistics;

import com.lancas.vswap.subproject.sandbox.ship.SandBoxServerShip;
import net.minecraft.server.level.ServerLevel;

import java.util.function.BiConsumer;
/*
public enum BallisticTerminateType {
    Embed()

    private BiConsumer<ServerLevel, SandBoxServerShip> onTerminated;

    private BallisticTerminateType(BiConsumer<ServerLevel, SandBoxServerShip> inOnTerminated) {
        onTerminated = inOnTerminated;
    }
}
*/